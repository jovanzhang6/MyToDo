# 测试报告：mytodo-stats-notify（统计 + 提醒 + 打包）
> 项目：mytodo · 切片 2/2
> 状态：通过（dev 形态；E 系安装验收待业主实测后终局确认）

## 测试范围与方式

- **产品类型**：同切片 1（Windows 桌面 Tauri）。
- **组合**：单测套件独立运行（25 个：切片 1 回归 15 + 本切片新 10）+ dev 运行验证。
- **没测什么**：E1–E4（安装/署名/自启/卸载保数据）待安装包实测；C1 面板交互、D3 实际 toast 呈现待业主在运行中的窗口手测（dev 形态 toast 署名仍是 PowerShell，属已知现象）。

## 用例与结果

| 用例 | 步骤 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| TC-11 单测全套（C2–C6/D1–D3 + 切片 1 回归） | 独立运行 cargo test | 全绿 | 25/25，含 b9 跨天等价性（daily_log 纳入 shape 后仍成立） | ✅ |
| TC-12 回归（切片 1 引擎+存储） | 同上套件的 b1–b10 | 全绿 | 15/15 | ✅ |
| TC-13 前端类型 | tsc --noEmit | 0 错误 | 0 错误 | ✅ |
| TC-14 统计纯度（C5） | c5_compute_stats_is_pure | 统计不产生副作用 | 通过（Debug 状态前后一致） | ✅ |
| TC-15 streak 三形态（C4） | 三个专项测试 | 连续/断零/None | 全过 | ✅ |
| TC-16 提醒去重（D1/D2） | d1/d2/d3 测试 | 同天一次、次日新任务再发、关闭即静默 | 全过 | ✅ |
| TC-17 面板交互（C1） | 业主手测 | 展开/收起/互斥 | **待业主确认** | ⏳ |
| TC-18 安装验收（E1–E4） | 业主实测安装包 | 署名/自启/卸载保数据 | **待安装包** | ⏳ |

## 缺陷

| 严重度 | 描述 | 处理 |
|---|---|---|
| 轻微 | snapshot_daily_log 借用检查失败（& 应为 &mut） | dev 当场修复，全量重跑通过 |
| 轻微 | stats 测试两处笔误（死代码/断言与口径矛盾） | QA 自查修正（QA 直接修复，不涉产品逻辑） |

## 回归影响

roll_over 追加 daily_log 快照 = 触碰切片 1 引擎核心。回归范围：TC-01 全套（b1–b10），结果 15/15 通过；b9 等价性测试更新 shape 后仍验证「一次跨多天 == 逐日重放」。

## 遗留风险

1. daily_log 只增不清（每天至多一条 <100B，10 年 <400KB，接受）。
2. dev 形态 toast 署名是 PowerShell（切片 1 已留痕），E2 在安装版终验。
3. 趋势图为 P1 实现（纯 div 条形，非 SVG），hover 用 title 提示——若业主想要更精细的图再迭代。
