# 测试报告：mytodo-stats-notify（统计 + 提醒 + 打包）
> 项目：mytodo · 切片 2/2
> 状态：通过（dev 形态；E 系安装验收待业主实测后终局确认）

## 测试范围与方式

- **产品类型**：同切片 1（Windows 桌面 Tauri）。
- **组合**：单测套件独立运行 + dev 运行验证 + 业主分轮手测（统计弹窗轮 → 统计/设置整页轮）。
- **2026-09-25 深夜更新**：统计由弹窗升级为独立整页（信息架构按 PM 三问重组）、设置由气泡升级为独立整页、daily_log 扩为全任务计数——单测由 25 调整为 24（stats 套件重写，覆盖面不减反增：streak 三形态+最长纪录、按期率、积压、热力图断档）。全量回归通过，TS 零错。
- **没测什么**：E1–E4（安装/署名/自启/卸载保数据）待业主晨检；统计/设置整页的视觉观感项待业主确认（当晚自主模式下以 24 单测+TS+代码评审为验收口径，见 decisions.md D13）。

## 用例与结果

| 用例 | 步骤 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| TC-11 单测全套（统计 9 + 提醒 4 + 切片 1 回归 15 = 24 + store 3） | 独立运行 cargo test | 全绿 | 24/24（深夜整页升级后重跑），含跨天等价性、streak 最长纪录、热力图断档 | ✅ |
| TC-12 回归（切片 1 引擎+存储） | 同上套件 b1–b10 | 全绿 | 全绿 | ✅ |
| TC-13 前端类型 | tsc --noEmit | 0 错误 | 0 错误 | ✅ |
| TC-14 统计页（C1–C6） | 弹窗轮业主手测通过；整页轮自动化口径（单测）+晨检观感确认 | 按验收标准 | 弹窗版 ✅；整页版单测 ✅、观感 ⏳ 晨检 | ✅/⏳ |
| TC-15 提醒逻辑（D1/D2/D3） | d1/d2/d3 单测 | 同天一次、次日再发、关闭静默 | 全过 | ✅ |
| TC-16 设置页 | 业主整页轮手测：滑杆/开关/置顶同步 | 即点即生效 | 业主确认「设置没有问题」 | ✅ |
| TC-17 视图切换回归 | 三视图互斥 + 再点回清单 | 干净切换 | 首轮发现 📊 双监听抵消（4baa48f）+ 僵尸引用（bbfaada）+ HMR 断链（进程重启根治），修复后待晨检终验 | ✅（修复后代码评审+单测口径） |
| TC-18 安装验收（E1–E4） | 业主晨检实测 | 署名/自启/卸载保数据 | **待业主** | ⏳ |

## 缺陷

| 严重度 | 描述 | 处理 |
|---|---|---|
| 一般 | 📊 点击无反应：视图切换监听在 titlebar/stats 双注册，两次 toggle 相互抵消 | 已回炉：删重复监听，切换权归一（`4baa48f`），晨检终验 |
| 一般 | 统计页切换报错：升级整页后 stats.ts 残留 settings-pop 僵尸引用 | 已回炉（`bbfaada`）；黑匣子 30 秒定位 |
| 一般 | 修复已做但窗口内仍是旧行为：HMR WebSocket 因窗口长期挂托盘断链，热更新送不进 | 流程回炉：进程级重启替代热重载判断（2 轮）；前端报错条加时间戳+6 秒自清防残影误判（`a3f70f6`） |
| 轻微 | snapshot_daily_log 借用检查失败；stats 测试笔误与 toggle 语义误用（三处） | dev/QA 当场修复，全量重跑通过 |
| 提示 | dev 形态 toast 借用 PowerShell 身份 | 非缺陷：安装版纠正（E2 验收项） |

## 回归影响

roll_over 追加 daily_log 快照 = 触碰切片 1 引擎核心。回归范围：TC-01 全套（b1–b10），深夜整页升级后重跑仍全绿；跨天等价性测试随 daily_log 计数制更新后继续成立。

## 遗留风险

1. daily_log 只增不清（每天至多一条 <100B，10 年 <400KB，接受）。
2. dev 形态 toast 署名是 PowerShell（切片 1 已留痕），E2 在安装版终验。
3. 趋势图为 P1 实现（纯 div 条形，非 SVG），hover 用 title 提示——若业主想要更精细的图再迭代。
